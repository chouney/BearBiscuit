<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.xkr.dao.mapper.XkrAdminPermissionMapper" >
  <resultMap id="BaseResultMap" type="com.xkr.domain.entity.XkrAdminPermission" >
    <!--
      WARNING - @mbg.generated
    -->
    <id column="id" property="id" jdbcType="INTEGER" />
    <result column="permission_name" property="permissionName" jdbcType="VARCHAR" />
    <result column="status" property="status" jdbcType="TINYINT" />
    <result column="create_time" property="createTime" jdbcType="TIMESTAMP" />
    <result column="update_time" property="updateTime" jdbcType="TIMESTAMP" />
    <result column="ext" property="ext" jdbcType="VARCHAR" />
  </resultMap>

  <sql id="permission_field" >
    id,permission_name,status,create_time,update_time,ext
  </sql>

  <select id="selectByIds" parameterType="String" resultMap="BaseResultMap">
    SELECT <include refid="permission_field"/>
    FROM xkr_admin_permission
    WHERE status = 1
    <if test="ids!=null and ids.size()!=0">
      AND id IN
      <foreach collection="ids" item="permissionId" index="index"
               open="(" close=")" separator=",">
        #{permissionId}
      </foreach>
    </if>
  </select>

  <select id="getAll"  resultMap="BaseResultMap">
    SELECT <include refid="permission_field"/>
    FROM xkr_admin_permission
    WHERE status = 1
  </select>

</mapper>