<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.xkr.dao.mapper.XkrAdminRoleMapper">
    <resultMap id="BaseResultMap" type="com.xkr.domain.entity.XkrAdminRole">
        <!--
          WARNING - @mbg.generated
        -->
        <id column="id" property="id" jdbcType="INTEGER"/>
        <result column="role_name" property="roleName" jdbcType="VARCHAR"/>
        <result column="permission_ids" property="permissionIds" jdbcType="VARCHAR"/>
        <result column="status" property="status" jdbcType="TINYINT"/>
        <result column="create_time" property="createTime" jdbcType="TIMESTAMP"/>
        <result column="update_time" property="updateTime" jdbcType="TIMESTAMP"/>
        <result column="ext" property="ext" jdbcType="VARCHAR"/>
    </resultMap>

    <sql id="role_field">
    id,role_name,permission_ids,status,create_time,update_time,ext
  </sql>


    <select id="selectById" parameterType="java.lang.Integer" resultMap="BaseResultMap">
        SELECT
        <include refid="role_field"/>
        FROM xkr_admin_role
        WHERE status = 1 AND id = #{id}
    </select>

    <update id="updateRoleById" parameterType="java.util.Map">
        UPDATE xkr_admin_role
        <trim prefix="set" suffixOverrides=",">
            <if test="roleName!=null">
                role_name = #{roleName} ,
            </if>
            <if test="permissionIds!=null">
                permission_ids = #{permissionIds} ,
            </if>
            <if test="ext!=null">
                ext = #{ext} ,
            </if>
        </trim>
        update_time = now()
        WHERE id = #{id}
    </update>

    <update id="batchUpdateRoleByIds" parameterType="java.util.Map">
        UPDATE xkr_admin_role
        SET status = #{status} , update_time = now() WHERE id IN
        <foreach collection="ids" item="id" separator="," open="(" close=")">
            #{id}
        </foreach>
    </update>

    <select id="selectList"  resultMap="BaseResultMap">
        SELECT
        <include refid="role_field"/>
        FROM xkr_admin_role
        WHERE status = 1
    </select>

    <select id="selectByIds" parameterType="java.util.List" resultMap="BaseResultMap">
        SELECT
        <include refid="role_field"/>
        FROM xkr_admin_role
        WHERE status = 1
        AND id IN
        <foreach collection="list" item="roleId" index="index"
                 open="(" close=")" separator=",">
            #{roleId}
        </foreach>
    </select>

</mapper>